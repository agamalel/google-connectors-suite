{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "message_id": {
          "type": "string"
        },
        "operation": {
          "type": "string",
          "enum": ["ack", "nack"]
        }
    }
}
